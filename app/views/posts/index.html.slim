h1 Listing posts

- @posts.each do |post|
  h2 = post.title
  p = post.content
  div
    b Author: 
    = post.user.email
    br
    = link_to 'Show', post, class: 'btn btn-default'
    = link_to 'Edit', edit_post_path(post), class: 'btn btn-info'
    = link_to 'Destroy', post, data: { confirm: 'Are you sure?' }, method: :delete, class: 'btn btn-danger'


= link_to 'New Post', new_post_path, class: 'btn btn-default'
